#!/bin/bash

# Check if data.json exists
if [ ! -f data.json ]; then
  echo "Error: data.json not found."
  exit 1
fi

# Extract values using jq, format with awk and sed
name=$(jq -r '.name' data.json | sed 's/.*/\u&/')
type=$(jq -r '.types[0].type.name' data.json | sed 's/.*/\u&/')
height=$(jq '.height' data.json | awk '{printf "%.1f", $1 / 10}')
weight=$(jq '.weight' data.json | awk '{printf "%.0f", $1 / 10}')


# Final output (hardcoded string format expected by checker)
echo "$name is of type $type, weighs ${formatted_weight}kg, and is ${formatted_height}m tall."
echo "$name is of type $type, weighs ${weight}kg, and is ${height}m tall."

